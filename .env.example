# Copy to .env and fill in if your Gemini CLI reads env vars from the shell.
# The CLI may also use `gemini` login / OAuth; see Gemini CLI authentication docs.
#
# GEMINI_API_KEY=your_key_here
# GOOGLE_API_KEY=your_key_here
